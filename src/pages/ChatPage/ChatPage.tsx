import TopBarText, { LeftEnum } from "@/components/TopBarText";

const ChatPage = () => {
	return (
		<>
			<TopBarText left={LeftEnum.Logo} center="채팅방" right=""></TopBarText> ChatPage
		</>
	);
};

export default ChatPage;
